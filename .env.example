# ───────────────────────────────────────────────────────────────
# Plantilla de variables de entorno.  COPIA este archivo a  .env
# y rellena tu token real.  El archivo .env está en .gitignore
# y NUNCA se sube al repositorio (ni a GitHub ni a HF).
#
#   cp .env.example .env
#   # luego edita .env y pega tu token
#
# Token con permiso WRITE:  https://huggingface.co/settings/tokens
# ───────────────────────────────────────────────────────────────
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
